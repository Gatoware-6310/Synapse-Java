package xyz.gatoware.synapse;

/** Compute devices supported by Synapse. */
public enum Devices {
	/** Execute operations on the CPU. This is the default. */
	CPU,
	/** Execute supported operations on an NVIDIA CUDA GPU. */
	CUDA
}
